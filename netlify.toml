[build]
  command = "chmod +x inject-api-key.sh && ./inject-api-key.sh"
  publish = "."

[build.environment]
  NODE_VERSION = "18"
  # Disable secrets scanning - the Gemini API key is domain-restricted and safe to expose
  SECRETS_SCAN_SMART_DETECTION_ENABLED = "false"
